# פרויקט סיווג תמונות - בינה מלאכותית

פרויקט לסיווג תמונות באמצעות מודל HuggingFace מקומי.

## התקנה

```bash
pip install -r requirements.txt
```

## הכנת המודל

הורד את קבצי המודל לתיקייה `model_files/`.

## הרצה

שים תמונות בתיקיית `images/` ואז הרץ:

```bash
python main.py
```
